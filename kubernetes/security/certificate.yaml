apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: backend-cert
  namespace: production
spec:
  secretName: backend-tls
  issuerRef:
    name: selfsigned
    kind: ClusterIssuer
  dnsNames:
    - myapp.local
